## Generated mta.yaml based on template version 0.4.0
## appName = cap-launchpad
## language=nodejs; multiTenant=false
## approuter=
_schema-version: '3.1'
ID: cap-launchpad
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install --production
      - npx -p @sap/cds-dk cds build --production

modules:
 - name: cap-launchpad-destination-content
   type: com.sap.application.content
   requires:
   - name: cap-launchpad-uaa
     parameters:
       service-key:
         name: cap-launchpad-uaa-key
   - name: cap-launchpad-html-repo-host
     parameters:
       service-key:
         name: cap-launchpad-html-repo-host-key
   - name: cap-launchpad-destination-service
     parameters:
       content-target: true        
   parameters:
     content:
       subaccount:
         destinations:
         - Name: cap-launchpad-html-repo-host
           ServiceInstanceName: fiori-html5-app-host-service
           ServiceKeyName: cap-launchpad-html-repo-host-key
           sap.cloud.service: my_service
         - Authentication: OAuth2UserTokenExchange
           Name: cap-launchpad-uaa
           ServiceInstanceName: fiori-xsuaa-service
           ServiceKeyName: cap-launchpad-uaa-key
           sap.cloud.service: my_service
         existing_destinations_policy: update    
   build-parameters:
     no-source: true
 - name: cap-launchpad-ui-deployer
   type: com.sap.application.content
   path: .
   requires:
   - name: cap-launchpad-html-repo-host
     parameters:
       content-target: true
   build-parameters:
     build-result: resources
     requires:
     - artifacts:
       - fiori-content.zip
       name: fiori
       target-path: resources/
 - name: fiori
   type: html5
   path: app/fiori
   build-parameters:
     builder: custom
     commands:
     - npm install
     - npm run build
     supported-platforms: []              
 # --------------------- SERVER MODULE ------------------------
 - name: cap-launchpad-srv
 # ------------------------------------------------------------
   type: nodejs
   path: gen/srv
   parameters:
     memory: 256M
     disk-quota: 1024M
   build-parameters:
     ignore: ["node_modules/"]
   requires:
    # Resources extracted from CAP configuration
    - name: cap-launchpad-db
    - name: cap-launchpad-uaa
   provides:
    - name: srv-api      # required by consumers of CAP services (e.g. approuter)
      properties:
        srv-url: ${default-url}

 # -------------------- SIDECAR MODULE ------------------------
 - name: cap-launchpad-db-deployer
 # ------------------------------------------------------------
   type: hdb
   path: gen/db
   parameters:
     buildpack: nodejs_buildpack
   build-parameters:
     ignore: ["node_modules/"]
   requires:
    # 'hana' and 'xsuaa' resources extracted from CAP configuration
    - name: cap-launchpad-db
    - name: cap-launchpad-uaa


resources:
 # services extracted from CAP configuration
 # 'service-plan' can be configured via 'cds.requires.<name>.vcap.plan'
# ------------------------------------------------------------
 - name: cap-launchpad-db
# ------------------------------------------------------------
   type: com.sap.xs.hdi-container
   parameters:
     service: hanatrial  # or 'hanatrial' on trial landscapes
     service-plan: hdi-shared
   properties:
     hdi-service-name: ${service-name}

 - name: cap-launchpad-destination-service
   type: org.cloudfoundry.managed-service
   requires:
     - name: srv-api
   parameters:
     service: destination
     service-name: cap-launchpad-destination-service
     service-plan: lite
     config:
       init_data:
         subaccount:
           existing_destinations_policy: update
           destinations:
             - Name: cap-launchpad
               Description: CAP sample service
               Authentication: NoAuthentication
               ProxyType: Internet
               Type: HTTP
               URL: ~{srv-api/srv-url}
               HTML5.DynamicDestination: true
               HTML5.ForwardAuthToken: true
       
 - name: cap-launchpad-html-repo-host
   type: org.cloudfoundry.managed-service
   parameters:
     service: html5-apps-repo
     service-name: fiori-html5-app-host-service
     service-plan: app-host
 - name: cap-launchpad-uaa
   type: org.cloudfoundry.managed-service
   parameters:
     path: ./xs-security.json
     service: xsuaa
     service-name: fiori-xsuaa-service
     service-plan: application



